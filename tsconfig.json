{
  "compilerOptions": {
    /* 기본 옵션 */
    "target": "ESNext",            // 컴파일할 JavaScript 버전을 최신으로 설정
    "module": "ESNext",            // 모듈 시스템을 최신으로 설정 (import/export)
    "jsx": "react-jsx",            // TSX/JSX 코드를 React 17+ 방식으로 변환하도록 설정

    /* 모듈 해석 관련 */
    "moduleResolution": "bundler", // 'node'와 유사하게 node_modules에서 라이브러리를 찾도록 설정
    "esModuleInterop": true,       // CommonJS 모듈과 ES 모듈 간의 호환성을 높임

    /* 엄격한 타입 검사 */
    "strict": true,                // 모든 엄격한 타입-체킹 옵션을 활성화 (코드 안정성 향상)

    /* 개발 편의성 */
    "skipLibCheck": true           // 설치된 라이브러리(node_modules)의 타입 검사는 건너뛰어 속도 향상
  },
  "include": [
    "index.tsx" // TypeScript 컴파일러가 관리할 파일 목록 (여기서는 index.tsx만 해당)
  ]
}